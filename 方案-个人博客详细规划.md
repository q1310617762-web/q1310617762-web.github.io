# 个人博客 · 详细开发方案

> 核心理念：**先模块化开发，最后合并**。每个模块独立可运行、独立可验证，最终通过统一的路由树与布局组装成完整站点。

---

## 0. 文档信息

| 项 | 内容 |
|---|---|
| 项目名称 | My Personal Blog |
| 目标 | 一个快速、可维护、SEO 友好、好看的个人技术/生活博客 |
| 开发方式 | 模块化 → 合并 |
| 交付形态 | 静态站点（可部署到 Vercel / GitHub Pages） |
| 版本 | v1.0（方案稿） |

---

## 1. 技术选型

### 1.1 框架：Astro 5.x
- **理由**：内容优先、默认零 JS、静态输出、SEO 极佳、构建快、生态成熟。
- Astro 的「Islands」架构允许我们在不需要 JS 的地方保持纯静态，只在需要交互的组件中注入 JS。

### 1.2 样式：Tailwind CSS 4.x
- 原子化类名，易于抽取「设计令牌」做主题统一。
- 深色模式用 `class` 策略 + CSS 变量实现。

### 1.3 UI 交互：Preact / Vue 3（按需 island）
- 仅用于：搜索、深色模式切换、复制按钮、目录高亮等交互点。
- 不需全站 SPA。

### 1.4 内容：Markdown + Content Collections
- 文章、分类、标签、归档全部由内容集合驱动。
- Schema 用 Zod 校验，编写内容时即可纠错。

### 1.5 搜索：Pagefind
- 构建时生成索引，免后端、支持中文、速度快。

### 1.6 评论：Giscus（基于 GitHub Discussions）或 Waline
- 默认 Giscus（免费、无后端、开源）。
- 若需要自有后端可切换 Waline。

### 1.7 部署：Vercel（首选）或 GitHub Pages
- Vercel：零配置、自动构建、支持预览。
- GitHub Pages：纯静态托管。

### 1.8 开发工具
- Node.js ≥ 20、pnpm
- 代码高亮：Shiki（Astro 内置）
- Markdown 渲染：`@astrojs/markdown-remark` / `remark-gfm` / `remark-reading-time`

---

## 2. 目标与性能指标

| 指标 | 目标 |
|---|---|
| Lighthouse 性能 | ≥ 95 |
| 首屏 JS | ≤ 50KB（不含需要交互的 island） |
| 构建时间 | 少量文章下 < 10s |
| 每篇页面加载 | 纯静态，无额外请求 |

---

## 3. 目录结构（合并后的最终形态）

```
my-blog/
├─ public/
│  ├─ favicon.svg
│  ├─ og-image.png
│  └─ robots.txt
├─ src/
│  ├─ components/          # 设计系统 + 业务组件
│  │  ├─ ui/               # 通用基础组件（按钮、卡片、徽章…）
│  │  ├─ blog/             # 博客业务组件（文章卡片、目录、标签…）
│  │  └─ layout/           # 头部、页脚、主题开关
│  ├─ layouts/
│  │  ├─ BaseLayout.astro
│  │  ├─ BlogLayout.astro
│  │  └─ PageLayout.astro
│  ├─ content/             # 内容集合（模块 C）
│  │  ├─ config.ts
│  │  └─ posts/            # Markdown 文章
│  ├─ pages/               # 路由（模块 D）
│  │  ├─ index.astro
│  │  ├─ about.astro
│  │  ├─ blog/
│  │  │  ├─ index.astro
│  │  │  ├─ [...page].astro
│  │  │  └─ [slug].astro
│  │  ├─ tag/[tag].astro
│  │  ├─ category/[category].astro
│  │  └─ rss.xml.js
│  ├─ styles/
│  │  └─ global.css        # 设计令牌 + 全局样式
│  ├─ utils/               # 工具函数（日期、阅读时长、slug…）
│  └─ data/                # 站点配置（作者、导航、社交链接）
├─ astro.config.mjs
├─ tailwind.config.mjs
├─ tsconfig.json
├─ package.json
└─ README.md
```

---

## 4. 模块化拆解（开发顺序即规划顺序）

下面每个模块都是**一个独立可验证的阶段**。模块之间有明确的接口边界，降低后期合并成本。

---

### 模块 A：项目脚手架（基础）

**目标**：搭出可运行的空壳，并建立**设计令牌**，供所有后续模块复用。

**任务清单**
1. 初始化 Astro + Tailwind + TypeScript，配置 `pnpm`。
2. 建立设计令牌（写入 `global.css` 的 CSS 变量）：
   - 颜色：primary / surface / text / border / accent（各含 light 与 dark 两套）
   - 字体：标题字族、正文字族、代码字族
   - 间距：4px 基准（`--space-1..8`）
   - 圆角、阴影、行高、容器最大宽度
3. 配置 `astro.config.mjs`：内容集合、站点 URL、shiki 主题。
4. 配置 `tailwind.config` 读取设计令牌。

**验收**：`pnpm dev` 能打开一个带主题变量的空白页，`pnpm build` 成功。

> 输出：`src/styles/global.css`、`src/data/site.ts`、配置文件。这是全站唯一依赖的基础。

---

### 模块 B：设计系统 / 组件库

**目标**：沉淀全站基础 UI 积木，可独立预览。

**任务清单**
- 基础组件（`src/components/ui/`）：
  - `Button.astro`（primary / ghost / outline 变体）
  - `Card.astro`
  - `Badge.astro`（标签用）
  - `Tag.astro`、`TagList.astro`
  - `Divider.astro`
  - `Avatar.astro`
- 布局组件（`src/components/layout/`）：
  - `Header.astro`（导航、logo、深色模式开关）
  - `Footer.astro`（社交链接、版权）
  - `ThemeToggle.astro`（island，localStorage 持久化）
  - `BackToTop.astro`
- 博客业务组件（`src/components/blog/`）：
  - `PostCard.astro`
  - `PostList.astro`
  - `Toc.astro`（文章目录，高亮当前章节）
  - `CodeBlock.astro`（代码高亮 + 复制）
  - `ReadingTime.astro`
  - `Pagination.astro`
  - `RelatedPosts.astro`
- 建立一个 `components/` 总览页或 Story 页作为独立预览台。

**验收**：在独立测试页看到每个组件在不同主题下渲染正确；深色模式正常切换。

---

### 模块 C：内容模型层

**目标**：定义内容结构与校验，保证所有页面数据来源一致。

**任务清单**
1. 在 `src/content/config.ts` 定义 Post Schema：
   ```ts
   title: string（必填）
   description: string（必填，用于 SEO/摘要）
   pubDate: date（必填）
   updatedDate?: date
   category: string（必填，单一分类）
   tags: string[]（默认空数组）
   cover?: image
   draft: boolean（默认 false）
   featured?: boolean
   ```
2. 提供辅助函数（`src/utils/`）：
   - `getAllPosts()`：读取、过滤 draft、按日期倒序
   - `getTags()` / `getCategories()` / `getArchives()`
   - `formatDate()` / `readingTime()`
3. 编写 6–10 篇**示例文章**（覆盖代码、图片、表格、标签、分类等多场景），用于后续页面验证与样式调试。
4. 配置 `@astrojs/sitemap` 生成站点地图。

**验收**：通过 `getAllPosts()` 能稳定拿到正确排序与过滤的文章集合；Zod 校验在内容出错时给出清晰报错。

---

### 模块 D：页面模块

**目标**：把内容呈现为完整页面。此阶段专注「页面结构」，暂不追求完美动效。

**任务清单**
1. **首页 `index.astro`**：Hero（简介、头像、CTA）+ 最新文章 + 分类入口 + 社交链接。
2. **文章列表 `blog/index.astro` 与分页 `blog/[...page].astro`**：卡片网格 + 分页。
3. **文章详情 `blog/[slug].astro`**：标题、元信息、封面、正文渲染、目录 TOC、上一/下一篇、相关文章、评论容器。
4. **标签页 `tag/[tag].astro`**：按标签聚合。
5. **分类页 `category/[category].astro`**：按分类聚合。
6. **归档页 `archive.astro`**：按年份归档。
7. **关于页 `about.astro`**：个人简介、技能、经历、联系方式。
8. **404 页 `404.astro`**。

**验收**：所有列表与详情页可正确导航、SEO（title/description/og）正确、无死链。

---

### 模块 E：功能模块

**目标**：补齐博客「好用」的关键能力。

**任务清单**
1. **搜索**：集成 Pagefind，输入即搜，中文支持。
2. **RSS**：`src/pages/rss.xml.js`，供订阅。
3. **深色模式**：`ThemeToggle` + 本地持久化 + 无闪烁（首屏内联脚本）。
4. **代码高亮与复制**：Shiki 语法高亮 + 右上角复制按钮（island）。
5. **图片优化**：`astro:assets` 自动压缩 + 懒加载。
6. **评论**：Giscus 组件（island）。
7. **阅读进度条**、**返回顶部**、**界面动效**（可选）。
8. **SEO/OG 图**：`@astrojs/sitemap` + 每页 meta + 默认 OG 图。

**验收**：搜索可用、RSS 输出正确、黑暗模式无刷新闪烁、代码可复制。

---

### 模块 F：交互与动效

**目标**：提升质感，让站点「活」起来。

**任务清单**
- 页面过渡动画（View Transitions，Astro 内置）
- 卡片 hover 阴影/上浮
- 目录滚动高亮
- 图片淡入
- 滚动出现动画（IntersectionObserver，注意性能与 `prefers-reduced-motion`）

**验收**：动效不阻塞首屏，禁用动画偏好时回退为无动画。

---

## 5. 「最后合并」策略（如何组装）

模块各自完成后，按以下顺序合并，确保无冲突、可整体构建。

1. **统一布局**：所有页面包进 `BaseLayout`（头部/页脚/主题上下文/SEO），嵌套 `BlogLayout`/`PageLayout`。
2. **路由组装**：在 `src/pages` 挂载模块 D 的全部页面与动态路由。
3. **组件注册**：从模块 B 统一 `import`，约定命名一致（PascalCase 组件 + kebab-case 文件名）。
4. **数据注入**：模块 C 的 `getAllPosts()` 等函数注入各模板。
5. **功能装配**：把模块 E 的搜索、RSS、评论、深色模式分别接到布局/详情页。
6. **设计令牌归一**：确认所有组件引用同一套 `global.css` 变量，避免散落魔法值。
7. **构建验证**：`pnpm build` 全量成功；`pnpm preview` 预览，检查：
   - 所有内部链接无死链
   - 分页、标签、分类路由正确
   - SEO meta 完整
   - dark/light 两种主题 OK
8. **性能体检**：Lighthouse 达标、首屏 JS 可控。

---

## 6. 数据流 / 接口边界

模块之间通过「内容集合」与「工具函数」解耦，降低合并耦合：

```
Markdown 内容 ──► Content Collections (Zod 校验) ──► 工具函数 getAllPosts/getTags
                                    │
        ┌───────────────────────────┐
        ▼                            ▼
   设计系统组件(UI)            页面模板(路由)
        └────────── 都由 design tokens 统一 ──────────┘
```

---

## 7. 分期交付（MVP 优先级）

| 阶段 | 内容 | 产出 |
|---|---|---|
| **P1 核心** | 模块 A、B、C、D | 可浏览的完整博客（列表/详情/首页/关于） |
| **P2 体验** | 深色模式、标签/分类/归档、代码高亮、RSS | 更完整的阅读体验 |
| **P3 进阶** | 搜索、评论、动效、阅读进度、SEO 增强 | 接近完美的成品 |

---

## 8. 里程碑 / 时间估算（单人，效率型）

| 里程碑 | 预估 |
|---|---|
| 脚手架 + 设计令牌（A） | 1 天 |
| 设计系统组件（B） | 1–2 天 |
| 内容模型 + 示例文章（C） | 1 天 |
| 核心页面（D） | 2 天 |
| 功能模块（E） | 1–2 天 |
| 动效 + 合并 + 调优（F + 合并） | 1–2 天 |

> 合计约 7–11 天（不包含文案撰写）。

---

## 9. 可扩展方向（后续）

- 置顶/精选文章
- 中英文双语
- 图片画廊
- 暗色下的配图主题
- 站点统计（如 Umami/Plausible）
- 评论的审核/回复通知
- PWA / 离线

---

## 10. 风险与对策

| 风险 | 对策 |
|---|---|
| 模块间样式冲突 | 统一设计令牌，组件只引用 CSS 变量 |
| 内容 Schema 变更导致页面报错 | 用 Zod 强校验 + 构建期报错 |
| 搜索/评论引入 JS 增大 | 用 island 按需挂载，默认零 JS |
| 后期合并冲突 | 先定目录与命名规范，再开发各模块 |
| 中文搜索效果差 | 用 Pagefind 并实测中文检索 |

---

## 11. 落地步骤（下一步）

1. **确认选型**（第 1 节）——是否就用 Astro + Tailwind + Preact/Vue。
2. 我从 **模块 A：脚手架 + 设计令牌** 开始搭建。
3. 每完成一步，用 `pnpm dev` 在本地跑通并截图/预览给你确认。
4. 全部模块完成后执行「合并」与整站构建验证。

> 若你希望跳过某些模块（如不要评论、不要搜索），直接告诉我，我会调整上述分期与工作量。
