# 上线部署 & 内容修改指南

本文分两部分：**① 怎么让别人看到你的博客（上线）**、**② 怎么改主页/文章内容**。

---

# 一、上线部署（让别人能访问）

## 方案对比（先看这个再动手）

| 平台 | 难度 | 费用 | 国内访问 | 需要备案 | 推荐度 |
|---|---|---|---|---|---|
| **腾讯云 EdgeOne Pages**（国内，⭐推荐） | ⭐⭐ 简单 | **永久免费** | ✅ 好（大陆可用区） | 默认域名不用；绑自定义域名要 | ⭐⭐⭐⭐⭐ |
| **Netlify** | ⭐ 最简单 | 免费 | 一般（可能慢） | 不用 | ⭐⭐⭐⭐ |
| **Vercel** | ⭐ 简单 | 免费 | 较差（默认域名常被墙） | 不用 | ⭐⭐⭐ |
| **GitHub Pages** | ⭐⭐ 中等 | 免费 | 一般 | 不用 | ⭐⭐⭐ |
| **阿里云 OSS / 腾讯云 COS** | ⭐⭐⭐ 较麻烦 | 极低价按量计费 | ✅ 快 | **必须备案** | ⭐⭐⭐ |
| ~~Gitee Pages~~ | — | — | — | — | ❌ **已下线** |

> ⚠️ **Gitee Pages 已经停止服务**（官方帮助页已 404），不要再用。
> ⚠️ 国内对象存储（OSS/COS）的**默认域名会强制下载 HTML**，必须绑定自定义域名才能当网站用 → 也就是**必须备案**。

---

## 🇨🇳 重点：国内免费方案 —— 腾讯云 EdgeOne Pages

这是目前**最值得用的国内免费托管**（官方定价页明确写「免费版永久提供，$0/月」）：

**免费版包含**：Git 部署、自动 CI/CD、全球 CDN、Edge Functions、KV/Blob 存储。
超出用量限制在收费版推出前**不会中断服务**。官方文档里有**专门的 Astro 框架指南**，原生支持。

### 加速区域怎么选（关键）

| 加速区域 | 国内访问速度 | 自定义域名备案 |
|---|---|---|
| **中国大陆可用区** | ✅ 快 | **必须工信部备案** |
| **全球可用区（不含大陆）** | 一般（走境外节点） | **无需备案** |

- 平台会给每个项目分配一个**默认子域名**，可直接访问分享（官方说明：非中国大陆网络环境可直接访问）
- 想国内稳定快速 → 选**中国大陆可用区** + 自己的域名 + **完成备案**
- 不想备案 → 选**全球可用区** + 自己的域名（免备案，速度一般但通常比 Vercel 稳）

### 部署步骤
1. 打开 https://pages.edgeone.ai → 注册/登录（腾讯云账号）
2. **导入 Git 仓库** → 授权 GitHub → 选你的 `my-blog`
3. 构建配置（一般会自动识别 Astro，如未识别就手填）：
   - 构建命令：`npm run build`
   - 输出目录：`dist`
   - Node 版本：`20`
4. 点部署 → 等 1~2 分钟 → 得到默认域名，**这就是你的博客地址**
5. （可选）域名管理 → 添加自定义域名 → 配置 CNAME + 免费 SSL 证书

### 各方案一句话总结
- **想最快看到效果**：Netlify（点几下就上线）
- **想国内免费且速度好**：EdgeOne Pages（大陆可用区）
- **已经有域名且愿意备案**：EdgeOne Pages 大陆可用区，或阿里云 OSS + CDN
- **完全不想折腾备案**：EdgeOne Pages 全球可用区 / Netlify / Vercel

> **重要提醒**：Vercel / Netlify / Cloudflare 的**默认域名**（`*.vercel.app`、`*.netlify.app`、`*.pages.dev`）在国内经常无法访问。
> 如果主要给**国内朋友**看，优先考虑 EdgeOne Pages。

---

## 第 0 步：把项目传到 GitHub（所有方案的前提）

1. 注册 [GitHub](https://github.com) 账号
2. 新建一个仓库，例如 `my-blog`（**选 Public 公开**，Pages 免费版需要公开）
3. 在本机项目目录 `E:\AI cahs\blog` 执行：

```bash
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/你的用户名/my-blog.git
git push -u origin main
```

> 如果本机没装 git，可以先去 https://git-scm.com 安装；或者用 GitHub Desktop 图形客户端。

---

## 方案 A：Netlify（最推荐，全程点鼠标）

1. 打开 https://app.netlify.com → 用 GitHub 账号登录
2. 点 **Add new site → Import an existing project**
3. 选 **GitHub** → 授权 → 选你的 `my-blog` 仓库
4. 构建配置会自动读取项目里的 `netlify.toml`（已配好），直接点 **Deploy**
5. 等 1~2 分钟，你会得到一个网址，如 `https://random-name.netlify.app` —— **这就是你的博客，可以发给别人了**
6. （可选）在 **Site configuration → Change site name** 改成好记的名字
7. （可选）**Domain management → Add custom domain** 绑定你自己的域名

**以后更新**：改完内容 `git push`，Netlify 自动重新部署。

---

## 方案 B：Vercel

1. 打开 https://vercel.com → 用 GitHub 登录
2. **Add New → Project** → 选你的 `my-blog` 仓库 → **Deploy**
3. 得到网址如 `https://my-blog.vercel.app`

> 项目里的 `vercel.json` 已配置好，无需手动改设置。

---

## 方案 C：GitHub Pages（全免费，用 GitHub 托管）

### C-1 准备：新建仓库

1. 注册/登录 [GitHub](https://github.com)
2. 右上角 **+ → New repository**
3. **Repository name** 建议填：**`你的用户名.github.io`**
   > 例：用户名是 `dyg`，就填 `dyg.github.io`
   > 这样网址是 `https://dyg.github.io/`（根路径，最省事）
   > 填别的名字也可以（如 `my-blog`），项目已做**子路径自动适配**，网址会是 `https://dyg.github.io/my-blog/`
4. 选 **Public（公开）** → **Create repository**

### C-2 上传文件（三种方式，选一种）

**方式一：网页上传（零安装，推荐先试这个）**
1. 进入刚建的仓库 → **Add file → Upload files**
2. 打开本机文件夹 `E:\AI cahs\github上传包\my-blog`
3. 把里面的**全部内容**拖进网页上传区 → 下方点 **Commit changes**
   > ⚠️ GitHub 网页**一次最多传 100 个文件**，本包共 129 个。
   > 如果提示超出，就分两批传：先传 `src`、`public`、`scripts` 等文件夹，再传根目录文件。
   > ⚠️ `.github` 是隐藏文件夹，拖拽时**可能漏掉**。如果没传上去，用方式二补上：
   > 在仓库点 **Add file → Create new file**，文件名填
   > `.github/workflows/deploy-pages.yml`，把本机
   > `E:\AI cahs\github上传包\my-blog\.github\workflows\deploy-pages.yml` 的内容粘进去，提交。
   > 同样再建一个 `.github/workflows/update-anime.yml`。

**方式二：GitHub Desktop（图形界面，最省心）**
1. 下载安装 https://desktop.github.com
2. 登录 → **File → Add local repository** → 选 `E:\AI cahs\github上传包\my-blog`
3. 提示不是仓库就点 **create a repository** → 发布时选 **Publish repository**（取消勾选 private）
4. 以后改完内容，点 **Commit → Push** 即可

**方式三：命令行（需先装 Git）**
```bash
# 先装 https://git-scm.com/download/win
cd "E:\AI cahs\github上传包\my-blog"
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/你的用户名/你的仓库名.git
git push -u origin main
```

### C-3 开启 Pages

1. 仓库 → **Settings → Pages**
2. **Source** 选 **GitHub Actions**（不要选 Deploy from a branch）
3. 回到仓库 **Actions** 标签 → 等「部署到 GitHub Pages」跑完（约 1~2 分钟）
4. 访问：`https://你的用户名.github.io/`（或 `.../仓库名/`）

> 项目里的 `.github/workflows/deploy-pages.yml` 会**自动识别**是「用户主页仓库」还是「普通项目仓库」，
> 自动设置正确的站点地址与子路径 —— 你不需要改任何配置。

### C-4 关于国内访问
`*.github.io` 在国内**可能较慢或偶尔打不开**。若主要面向国内访客，建议：
- 在 Pages 里绑定**自己的域名**（Settings → Pages → Custom domain），并可在仓库
  **Settings → Secrets and variables → Actions → Variables** 里新增 `SITE_URL` 变量填你的域名
- 或改用前面提到的**腾讯云 EdgeOne Pages**

---

## 自动更新新番列表（已配好，无需操作）

`.github/workflows/update-anime.yml` 会**每天自动**抓取 B 站番剧更新表并提交。
只要项目在 GitHub 上，托管平台检测到提交就会自动重新部署 —— **你完全不用管**。

想立刻更新一次：仓库 → Actions → “自动更新新番列表” → **Run workflow**。

---

## 绑定自己的域名（可选，但推荐）

1. 买域名：阿里云 / 腾讯云 / Namecheap 等
2. 在托管平台添加自定义域名，按提示到域名商配置 DNS：
   - `A` 记录指向平台给的 IP，或
   - `CNAME` 记录指向平台给的域名
3. 等待生效（几分钟到几小时），平台会自动签发 HTTPS 证书
4. **国内平台/国内访问**：域名需要 **ICP 备案**才能用国内服务器

---

# 二、怎么改内容

## 2.1 改主页各块内容 —— 对照表

**几乎所有主页文案都在一个文件里**：`src/data/site.ts`

| 主页上的位置 | 改哪里 | 具体字段 |
|---|---|---|
| 左侧栏头像 | `src/data/site.ts` | `site.avatar`（图片放 `public/` 下）|
| 左侧栏站名 | `src/data/site.ts` | `site.author` |
| 站名下方一句话 | `src/data/site.ts` | `site.tagline` |
| **大标题**「在游戏与音乐里寄存日常」 | `src/data/site.ts` | `hero.headline` |
| 副标题「游戏 · 音乐 · 日常随笔…」 | `src/data/site.ts` | `hero.sub` |
| 引言「把喜欢的东西，认真记下来。」 | `src/data/site.ts` | `hero.lead` |
| 两个按钮的文字/链接 | `src/data/site.ts` | `hero.ctas` |
| 导航菜单（首页/博客/相册…） | `src/data/site.ts` | `nav` |
| 侧栏底部链接（B站/QQ/邮箱/RSS） | `src/data/site.ts` | `social` |
| 首页「联系方式」卡片 | `src/data/site.ts` | `contacts` |
| 微信号 | `src/data/site.ts` | `wechat.id`、`wechat.qr` |
| 左下角音乐播放器歌单 | `src/data/site.ts` | `music`（音频放 `public/music/`）|
| 相册（图片墙） | `src/data/site.ts` | `albums` |
| 友链页 | `src/data/site.ts` | `friends` |
| 新番更新列表 | **自动**，无需改 | 见上方「自动更新」|
| 背景壁纸 | `public/wallpaper.jpg` | 直接替换该图片文件 |

> **「精选文章」和「最新文章」不用手动排**：
> - **精选** = 文章 frontmatter 里写了 `featured: true` 的文章
> - **最新** = 按文章日期自动倒序

## 2.2 改文章的两种方式

### 方式一：在 GitHub 网页上直接改（**推荐，不用装任何东西**）

1. 打开你的 GitHub 仓库
2. 进入 `src/content/posts/` 目录
3. **改文章**：点开某个 `.md` 文件 → 点右上角铅笔 ✏️ → 修改 → 拉到底点 **Commit changes**
4. **加文章**：点 **Add file → Create new file** → 文件名写 `我的新文章.md` → 粘贴下面的模板 → Commit
5. 提交后平台会**自动重新部署**，1~2 分钟后线上就更新了

**文章模板**（直接复制）：

```markdown
---
title: 文章标题
description: 一句话摘要（会显示在卡片上，也用于搜索引擎）
pubDate: 2026-10-01
category: 日常
tags: [随笔, 游戏]
featured: false
---

这里开始写正文，用 Markdown 语法。

## 二级标题

- 列表项
- 列表项

**加粗**、*斜体*、[链接](https://example.com)

> 引用一句话
```

**字段说明**：

| 字段 | 必填 | 说明 |
|---|---|---|
| `title` | ✅ | 标题 |
| `description` | ✅ | 摘要 |
| `pubDate` | ✅ | 日期，格式 `2026-10-01` |
| `category` | ✅ | 分类（如 游戏 / 音乐 / 日常 / 关于我）|
| `tags` | ❌ | 标签数组，如 `[原神, 音乐]` |
| `featured` | ❌ | 写 `true` 就会出现在首页「精选文章」|
| `cover` | ❌ | 封面图路径，如 `/cover.jpg` |
| `draft` | ❌ | 写 `true` 表示草稿，不会发布 |

### 方式二：在本机改（适合批量修改）

```bash
cd "E:\AI cahs\blog"
npm run dev          # 本地预览 http://localhost:4321（改完自动刷新）
# ...编辑文件...
npm run build        # 构建
git add . && git commit -m "更新内容" && git push   # 推送后线上自动更新
```

## 2.3 换图片（头像 / 壁纸 / 相册 / 封面）

1. 在 GitHub 网页版进入 `public/` 目录
2. **Add file → Upload files** 上传图片
3. 然后在 `src/data/site.ts`（或文章 frontmatter）里把路径改成 `/你的文件名.jpg`

| 想换什么 | 放哪里 | 怎么生效 |
|---|---|---|
| 头像 | `public/avatar.jpg` | 覆盖同名文件即可 |
| 背景壁纸 | `public/wallpaper.jpg` | 覆盖同名文件即可 |
| 游戏封面 | `public/games/*.jpg` | 保持文件名覆盖 |
| 相册图片 | `public/gallery/*.jpg` | 覆盖 + 在 `site.ts` 的 `albums` 里登记 |
| 音乐 | `public/music/*.m4a` | 覆盖 + 在 `site.ts` 的 `music` 里登记 |
| 微信二维码 | `public/wechat.png` | 放进去即可（配置已指向它）|

---

# 三、相册自动同步（往文件夹丢图即可）

## 它怎么工作

```
你把图片丢进  C:\Users\VOS-User\Pictures\Saved Pictures
        ↓   （包括子文件夹，除「博客壁纸」外）
Windows 计划任务「博客相册自动同步」每 30 分钟检查一次
        ↓
自动压缩为网页尺寸 → 更新相册数据 → 提交并推送到 GitHub
        ↓
GitHub Actions 自动构建 → 网站相册更新（约 1~2 分钟）
```

**你唯一要做的**：把图片放进那个文件夹。**其他全自动。**

## 相册页面

- 自动同步的图片会出现在相册页最前面的 **「随手拍」** 相册里
- 按文件时间倒序排列，最多展示 **60 张**（超出上限的旧图不展示，但源文件保留）
- 同一个文件（内容相同）只会导入一次，不会重复

## 手动触发（想立刻更新时）

双击运行：
```
E:\AI cahs\blog\scripts\auto-publish.cmd
```
或命令行：`cd "E:\AI cahs\blog"` → `npm run publish:photos`

## 想调整配置

| 想改什么 | 改哪里 |
|---|---|
| 换成别的图片文件夹 | 设环境变量 `PHOTO_DIR`，或改 `scripts/sync-photos.mjs` 里的 `SRC` |
| 最多展示多少张 | 设 `PHOTO_MAX`（默认 60），或改 `sync-photos.mjs` 里的 `MAX_PHOTOS` |
| 排除某些子目录 | 改 `sync-photos.mjs` 里的 `IGNORE_DIRS`（默认排除「博客壁纸」）|
| 检查频率 | 任务计划程序里改「博客相册自动同步」的触发器（默认 30 分钟）|

## 查看运行记录

```
E:\AI cahs\blog\logs\auto-publish.log
```
每次运行都会记录：扫描到几张、转换了几张、是否推送成功。

## 注意

1. **电脑要开机**才会执行（关机期间不会同步，开机后 `StartWhenAvailable` 会补跑一次）
2. **同步依赖 GitHub 凭据**（已存入 Windows 凭据管理器）。如果令牌过期，日志会显示「推送失败（可能是凭据失效）」，此时需要重新配置凭据
3. **想暂停自动同步**：任务计划程序里右键「博客相册自动同步」→ 禁用
4. **仓库体积**：图片会压缩到约 100~250KB/张，60 张约 10MB 左右

---

# 五、常见问题

**Q：改了内容线上没变化？**
→ 去托管平台看部署是否完成（Netlify/Vercel 有 Deploys 页面）；GitHub Pages 看 Actions 是否成功。通常等 1~2 分钟。

**Q：新番列表不更新？**
→ 仓库 → Actions → 看 “自动更新新番列表” 是否成功。可手动 **Run workflow** 立即更新。

**Q：别人打开很慢 / 打不开？**
→ 大概率是国内访问国外托管的问题，见开头「方案对比」的提醒，建议绑自有域名或换国内托管。

**Q：想改主题配色 / 版式？**
→ 三套主题的配色在 `src/styles/global.css`（搜 `--color-primary` 等变量）；版式在 `src/layouts/BaseLayout.astro` 与 `src/components/layout/SiteSidebar.astro`。

**Q：音乐/图片太大？**
→ 音乐建议用 `.m4a`（AAC 96k）；图片建议 ≤1600px 宽、JPEG 质量 80 左右。

---

# 四、常用命令速查

```bash
npm run dev            # 本地预览（改代码实时刷新）
npm run build          # 构建到 dist/
npm run preview        # 预览构建结果
npm run fetch:anime    # 手动更新新番列表
```
